'use strict';

var React = require('react'),
    ManageAuthorPage = React.createClass({
        render: function() {
            return (
                <h1>Manage Author</h1>
            );
        }
    });

module.exports = ManageAuthorPage;